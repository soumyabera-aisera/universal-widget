name: Build and Package Zendesk App

on:
  workflow_dispatch:
    inputs:
      platform:
        description: 'Platform to build for (e.g., zendesk)'
        required: true
        default: 'zendesk'

jobs:
  build-and-package-zendesk:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Widget Repo
        uses: actions/checkout@v4
        with:
          path: widget-repo

      - name: Checkout Templates Repo
        uses: actions/checkout@v4
        with:
          repository: soumyabera-aisera/widget-templates
          path: templates-repo

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20.x' # Updated to a version compatible with ZCLI

      - name: Build Universal Widget
        run: |
          cd widget-repo/universal-widget
          npm install
          npx stencil build

      - name: Assemble App for Packaging
        run: |
          # 1. Create a clean directory for the final app package
          mkdir -p ./app_dist/assets/build

          # 2. Copy the manifest and translations to the app root
          cp ./templates-repo/zendesk-template/manifest.json ./app_dist/
          cp -r ./templates-repo/zendesk-template/translations ./app_dist/

          # 3. Copy all template assets (logos, index.html) into the assets folder
          cp -r ./templates-repo/zendesk-template/assets/* ./app_dist/assets/

          # 4. Copy the compiled widget code into the assets folder
          cp -r ./widget-repo/universal-widget/dist/* ./app_dist/assets/build

      - name: Install ZCLI
        run: npm install @zendesk/zcli -g

      - name: Package Zendesk App with ZCLI
        env:
          ZENDESK_SUBDOMAIN: ${{ secrets.ZENDESK_SUBDOMAIN }}
          ZENDESK_EMAIL: ${{ secrets.ZENDESK_EMAIL }}
          ZENDESK_API_TOKEN: ${{ secrets.ZENDESK_API_TOKEN }}
        run: zcli apps:package ./app_dist

      - name: Upload Zendesk App Artifact
        uses: actions/upload-artifact@v4
        with:
          name: zendesk-app-package
          path: ./app_dist/tmp/*.zip